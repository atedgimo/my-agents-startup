# Pulse 2026-08-04 03:26

**Trigger:** 2 card(s) awaiting verification  
**Dispatched to:** startup-qa

## Task

Cards #0020, #0021 are in review. Verify each: read the code, write/extend tests under tests/, run run_tests. Move genuinely working cards to done with update_issue; for real failures file a 'bug' issue and move the card back to in-progress with a comment.

Note: 4 question(s) await the co-founder. Do not block on them — proceed with your best judgement and state your assumption.

## Outcome

Cards #0020 and #0021 are in review but have blocking issues:

- Card #0020 (ghost visual identifiers & state logic backend) lacks the backend implementation file (expected src/backend/ghosts.py or similar) and test files. There is an open bug #0050 about missing tests and implementation blocking release. The backend main.py does not show ghost-related API endpoints or logic for this feature.

- Card #0021 (ghost identity & state visuals frontend) has no visible frontend file src/frontend/ghosts.html or related test files. The card notes a timeout error in tests and references bug #0050 as blocking.

The product map shows no routes or modules related to ghosts. The backend main.py does not include ghost state logic or API routes for ghost visuals.

The existing game.js implements motion interpolation and basic game rendering but no ghost identity or state visuals.

The bug #0050 blocks release of card #0020 and indirectly #0021. It requires adding missing backend implementation and tests for ghost visual identifiers and state logic.

Therefore, I cannot verify these cards as done. I will move card #0020 back to in-progress with a comment referencing bug #0050 and do the same for card #0021, noting the missing frontend files and test failures.

Summary:
- Card #0020 backend implementation and tests missing, blocks release.
- Card #0021 frontend files missing, test failures, blocked by #0050.
- Move both cards back to in-progress with comments.
- No tests to run successfully, so no run_tests yet.

Proceeding to update the cards accordingly.
