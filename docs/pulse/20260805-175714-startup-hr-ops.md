# Pulse 2026-08-05 17:57

**Trigger:** 64 open cards for 5 builder(s) — understaffed  
**Dispatched to:** startup-hr-ops

## Task

The board has 64 open cards but only 5 agent(s) who build them. Read the board with list_issues to see what KIND of work dominates IMPORTANT: every agent shares ONE local model, so hiring does NOT add throughput — it only adds memory pressure and makes every cycle slower. Prefer rebalancing work or fire_agent on a redundant role. Only call hire_agent if a genuinely missing SKILL blocks work, and the company is under its headcount cap (check company_headcount). Report what you decided and why.

## Outcome

The product map shows several backend modules and routes. Notably:

- Several backend routes are not called by anything, including:
  - POST /input
  - GET /move
  - POST /clear_input
  - GET /startups
  - GET /pellets
  - POST /collect_pellet

- Several backend modules are imported by nothing:
  - src/backend/collision.py
  - src/backend/ghost_ai.py
  - src/backend/input_buffer.py
  - src/frontend/motionInterpolation.js

This indicates some dead or orphaned code that is not currently integrated or used by the frontend or other parts of the system. This could be causing confusion, duplicated effort, or wasted maintenance time.

Given the current understaffing and workload imbalance, I recommend the following actions:

1. File a chore card to audit and clean up unused backend routes and modules to reduce technical debt and simplify the codebase.
2. Review the workload distribution with the tech lead to see if some agents can shift focus from backend maintenance to active feature work.
3. Since no headcount room exists, no hiring is possible. Consider if any roles are redundant or underutilized for possible firing, but this requires more detailed workload and role analysis.

I will first create a chore card for the audit and cleanup of unused backend routes and modules.
