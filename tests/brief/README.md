# The brief, as running code — co-founder owned

These five probes ARE the company's score. Each one is a criterion from
BOARD_BRIEF.md made executable. `update_issue` runs them when a card closes:
a close that would make the score go DOWN is refused, so deleting your way
past a gate no longer works. Agents cannot edit anything in this directory —
change the product, not the measure.
